****Copy and paste this into router but change values

enable
configure terminal

no ip domain-lookup

line con 0
logging synchronous
exit

! Set privileged exec mode (enable) password
enable secret cisco

! Set hostname and domain (needed for SSH)
hostname R#
ip domain-name gns3.local

! Create a local login user (used for SSH login)
username admin privilege 15 secret admin123

! Generate SSH RSA key pair
crypto key generate rsa modulus 2048

! Enable SSH version 2
ip ssh version 2

! Configure VTY (virtual terminal lines) for SSH access
line vty 0 4
 login local
 transport input ssh
 exec-timeout 10 0
exit


! Save config
end
write memory
